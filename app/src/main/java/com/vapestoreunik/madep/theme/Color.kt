package com.vapestoreunik.madep.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────────────────────────────────────
// Kasir Vapestore — Premium Onyx + Gold
// Black-on-gold luxury boutique. Inspired by Rolex/Hublot — deep onyx primary,
// premium amber-gold CTA, warm cream surfaces.
// ─────────────────────────────────────────────────────────────────────────────

// ── ONYX SCALE (warm black — primary surfaces, text, hero cards) ─────────────
val BrandOnyx950 = Color(0xFF0C0A09) // deepest — hero card bg, primary CTA text
val BrandOnyx900 = Color(0xFF1C1917) // primary — app bar, headings, body text
val BrandOnyx800 = Color(0xFF292524) // surface dark mode
val BrandOnyx700 = Color(0xFF44403C) // outline dark, dividers dark mode
val BrandOnyx600 = Color(0xFF57534E) // secondary text light mode
val BrandOnyx400 = Color(0xFFA8A29E) // muted text dark mode, light outline
val BrandOnyx200 = Color(0xFFE7E5E4) // light divider
val BrandOnyx100 = Color(0xFFF5F5F4) // subtle container, surface variant light

// ── GOLD SCALE (CTA + brand accent — premium amber-gold) ─────────────────────
val BrandGoldRoyal = Color(0xFFD4AF37) // hero accents only (sparingly — wordmark detail)
val BrandGold700 = Color(0xFF92400E)   // deep gold container text
val BrandGold600 = Color(0xFFCA8A04)   // ★ PRIMARY CTA — Bayar, Selesaikan, Simpan
val BrandGold500 = Color(0xFFEAB308)   // hero value display, dark mode CTA
val BrandGold400 = Color(0xFFFACC15)   // highlight, accent details
val BrandGold300 = Color(0xFFFDE68A)   // tertiary container (subtle highlight)
val BrandGold200 = Color(0xFFFEF3C7)   // light gold container
val BrandGold100 = Color(0xFFFFFBEB)   // lightest gold tint

// ── CREAM (warm off-white — backgrounds, light surfaces) ─────────────────────
val BrandCream50 = Color(0xFFFAFAF9)  // main background, on-dark text
val BrandCream100 = Color(0xFFF5F5F4) // surface variant light
val BrandCream200 = Color(0xFFE7E5E4) // subtle divider light

// ── STATUS (semantic — not part of brand identity) ───────────────────────────
val BrandRed600 = Color(0xFFDC2626)
val BrandRed400 = Color(0xFFF87171)
val BrandRed100 = Color(0xFFFEE2E2)
val BrandGreen600 = Color(0xFF16A34A)
val BrandGreen400 = Color(0xFF4ADE80)
val BrandGreen100 = Color(0xFFDCFCE7)
