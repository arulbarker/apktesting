package com.vapestoreunik.madep.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────────────────────────────────────
// Kasir Vapestore — BLACKOUT YELLOW
// Pure black canvas + electric yellow brand. Streetwear-luxe / cyberpunk-premium.
// Single brand-locked scheme — light/dark system setting is ignored.
// ─────────────────────────────────────────────────────────────────────────────

// ── BLACK SCALE (canvas, surfaces, text) ─────────────────────────────────────
val BrandBlack = Color(0xFF000000)        // pure — rare, accent strokes only
val BrandJet = Color(0xFF0A0A0A)          // ★ main background — near-black warm
val BrandCarbon = Color(0xFF141414)       // ★ card surface
val BrandSlate = Color(0xFF1F1F1F)        // raised surface (FAB, dialog)
val BrandIron = Color(0xFF2A2A2A)         // hover / press / subtle dividers
val BrandSteel = Color(0xFF404040)        // visible outlines
val BrandSmoke = Color(0xFF71717A)        // muted / helper text
val BrandAsh = Color(0xFFA1A1AA)          // secondary text
val BrandSnow = Color(0xFFFAFAFA)         // ★ primary text on dark
val BrandPaper = Color(0xFFF4F4F5)        // rare — for light surfaces

// ── ELECTRIC YELLOW (THE brand color — used liberally) ───────────────────────
val BrandYellow = Color(0xFFFACC15)       // ★ PRIMARY BRAND — buttons, hero numbers, key accents
val BrandYellowHi = Color(0xFFFDE047)     // highlight / hover state
val BrandYellowDeep = Color(0xFFEAB308)   // pressed / active state
val BrandYellowGlow = Color(0xFFFFFB47)   // ultra-bright — thin strokes, edge accents
val BrandYellowDim = Color(0xFF713F12)    // muted container (for "yellow background area" usage)

// ── STATUS ───────────────────────────────────────────────────────────────────
val BrandRed = Color(0xFFEF4444)
val BrandRedDim = Color(0xFF7F1D1D)
val BrandGreen = Color(0xFF22C55E)
val BrandGreenDim = Color(0xFF14532D)
